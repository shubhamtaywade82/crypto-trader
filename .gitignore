# Logs
*.log

# UI
ui/node_modules/
ui/dist/
ui/.env
ui-solidjs-backup/

# Local state
wallet_*.json
wallet_*.db
wallet_events_*.jsonl
ledger_dev.db
*.db

# General
.antigravitycli/**
llm_cache/**

# ── Environment files ──
# Gitignored (contain real secrets / local overrides):
.env
.env.secrets
# Tracked (safe templates with placeholder values — commit these):
!.env.example
!.env.secrets.example

# Python caches
**/__pycache__/
.pytest_cache/

# Scratch / legacy files (not part of ship)
scratch/

# Symlinks (bin -> scripts)
bin

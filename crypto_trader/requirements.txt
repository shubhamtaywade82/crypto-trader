pandas>=1.5.0
numpy>=1.23.0
requests>=2.28.0
websocket-client>=1.6.0
python-telegram-bot>=20.0
python-dotenv>=1.0.0
# Production event journal (Postgres). psycopg2 is imported lazily; only
# required when DATABASE_URL is set.
psycopg2-binary>=2.9.0
# Redis Streams signal/execution pipeline. Imported lazily; only required when
# EXECUTION_BUS=redis.
redis>=5.0.0
# Dashboard API (read-only). Only needed to run crypto_trader.api.app.
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
# httpx backs starlette's TestClient. <0.28 because 0.28 removed the app=
# shortcut TestClient relies on (breaks tests/test_api.py).
httpx>=0.26,<0.28

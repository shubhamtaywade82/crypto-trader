pandas>=1.5.0
numpy>=1.23.0
requests>=2.28.0
websocket-client>=1.6.0
python-telegram-bot>=20.0
python-dotenv>=1.0.0
# Production event journal (Postgres). psycopg2 is imported lazily; only
# required when DATABASE_URL is set.
psycopg2-binary>=2.9.0
# Redis Streams signal/execution pipeline. Imported lazily; only required when
# EXECUTION_BUS=redis.
redis>=5.0.0
